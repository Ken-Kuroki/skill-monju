interface:
  display_name: "Monju"
  short_description: "Launch persistent OpenCode Go reviews"
  default_prompt: "Use $monju to launch parallel read-only OpenCode Go reviews in a tracked tmux session, notify me when they finish, and collect the results in a later turn."
