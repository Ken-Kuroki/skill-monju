interface:
  display_name: "Monju"
  short_description: "Run selected Cursor, OpenCode, or manual reviews"
  default_prompt: "Use $monju. First ask me which exact backend/model pairs to use; never choose defaults. Then launch the selected parallel read-only Cursor or OpenCode reviews in tracked tmux sessions, support manual handoffs for non-CLI models, and collect the results in a later turn."
